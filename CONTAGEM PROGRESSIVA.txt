let numeroLimite = prompt('Digite um número para iniciar a contagem progressiva: ');
let contador = 0;

while (contador <= numeroLimite) {
    console.log(`Contagem progressiva: ${contador}`);
    contador++;
} 